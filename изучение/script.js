'use strict';

const booking = [];

const makeBooking = function(flightNum, passengerName, price) {
    const booking ={
        flightNum, 
        passengerName, 
        price
    };
    console.log(booking);
    booking.push(booking);
}